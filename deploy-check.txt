배포 확인용 파일입니다.

이 파일이 브라우저에서 보이는데 /api/regions 가 404라면,
정적 파일 배포는 되고 있지만 Cloudflare Pages Functions가 실행되지 않는 상태입니다.
(GitHub Pages로 배포 중이거나, Pages 프로젝트가 이 저장소에 연결되지 않은 경우)

이 파일도 404라면 아직 배포가 돌지 않은 것입니다.

확인이 끝나면 지워도 되는 파일입니다.
